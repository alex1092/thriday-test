import { View, Text } from 'react-native';

export const TaxScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Tax Screen</Text>
    </View>
  );
};
